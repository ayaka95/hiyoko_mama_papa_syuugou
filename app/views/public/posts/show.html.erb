<div class="container">
  <div class="row">
    <div class="col-7">
      <h1>投稿詳細</h1>
      <div class="card  mx-auto mt-4 shadow-lg">
        <div class="row mx-4">
          <p class="mt-4"><%= image_tag @post.user.get_profile_image(60,60), class: :"rounded-circle" %></p>
          <h4 class="ml-3 mt-5"><%= @post.user.name %></h4>
        </div>
        <p class="mx-auto"><%= image_tag @post.get_image(400,400) %></p>
        <!--投稿画像のサイズが画面幅に対応しない。モデルで高さと幅の引数を指定したせい？-->
        <h4 class="mx-4">タイトル: <%= @post.title %></h4>
        <p class="mx-4">本文:<%= @post.body %></p>
      </div>
      <h4>
        <% if @post.user == current_user %>
          <%= link_to "削除", post_path(@post), method: :delete, class: "btn btn-md btn-danger", "data-confirm" => "投稿を削除しますか？" %>
          <%= link_to "編集", edit_post_path(@post), class: "btn btn-md btn-success m-2" %>
        <% end %>
      </h4>
    </div>
    <div class="col-5">
      <div>
        <h1>コメント</h1>
        <%= form_with model: [@post, @post_comment], class: "mt-4" do |f| %>
          <%= f.text_area :comment, rows: '7', placeholder: "50文字以内でコメントしてください", class: "w-100" %>
          <%= f.hidden_field :post_id, value: @post.id %></br>
          <%= f.submit "送信する" %>
        <% end %>
      </div>
      <h4 class="mt-5"><%= @post.post_comments.count %>件のコメント</h4>
      <table class="table">
        <thead>
          <tr>
            <th scope="col"></th>
            <th scope="col">ユーザー画像</th>
            <th scope="col">名前</th>
            <th scope="col">日付</th>
            <th scope="col">コメント内容</th>
          </tr>
        </thead>
        <% @post.post_comments.each do |post_comment| %>
        <tbody>
          <tr>
            <th scope="row">
              <td><%= image_tag post_comment.user.get_profile_image(60,60), class: :"rounded-circle" %></td>
              <td><%= post_comment.user.name %></td>
              <td><%= post_comment.created_at.strftime('%Y/%m/%d') %></td>
              <td><%= post_comment.comment %></td>
            </th>
          </tr>
        </tbody>
        <% end %>
      </table>
    </div>
  </div>
</div>